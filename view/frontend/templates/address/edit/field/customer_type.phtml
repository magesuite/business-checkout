<?php
/**
 * @var $block \MageSuite\BusinessCheckout\Block\Address\Edit\Field\CustomerType
 */
$selectedValue = $block->getCustomerTypeValue();
?>

<div class="field field-customer-type">
    <label class="label" for="customer_type">
        <span><?= $block->escapeHtml(__('Customer Type')) ?></span>
    </label>

    <div class="control">
        <select name="customer_type" id="customer_type">
            <?php
            foreach($block->getOptions() as $option){
                $selected = $option['value'] == $selectedValue ? 'selected="selected"' : '';
                ?><option value="<?= $option['value']; ?>" <?= $selected; ?>><?= __($option['label']); ?></option><?php
            }
            ?>
        </select>
    </div>
</div>
